/**
 * The canonical Exness referral link. It's public by nature (meant to be
 * shared everywhere), so it's safe to commit — no need to configure it in
 * Vercel for the site to work correctly out of the box.
 *
 * Override it via NEXT_PUBLIC_EXNESS_REFERRAL_URL only if you need to swap
 * it later (a different partner ID, a campaign-specific link, etc.).
 */
export const DEFAULT_EXNESS_REFERRAL_URL =
  "https://one.exnesstrack.net/boarding/sign-up/a/hxi3m8ietg?lng=es";
